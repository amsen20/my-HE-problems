#include<bits/stdc++.h>
#include "testlib.h"
using namespace std;

const int MAXN = 1e4;
const long long MAXM = 1e18;

int main(int argc , char* argv[])
{
	registerValidation(argc, argv);
    int n = inf.readInt(2, MAXN);
    inf.readSpace();
    long long sum = inf.readLong(1, MAXM);
    inf.readEoln();
    inf.readEof();
}
